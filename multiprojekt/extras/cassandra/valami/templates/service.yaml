apiVersion: v1
kind: Service
metadata:
  name: {{ include "valami.fullname" . }}
  labels:
    {{- include "valami.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "valami.selectorLabels" . | nindent 4 }}
