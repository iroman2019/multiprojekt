apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "valami.fullname" . }}-test-connection"
  labels:
    {{- include "valami.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "valami.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
